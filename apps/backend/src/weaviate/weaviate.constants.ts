export {};
// This file is intentionally created to be minimal or empty.
// Its primary purpose is to exist so that it can be mocked by Jest tests (e.g., in weaviate.service.spec.ts).
// The actual constants used during tests are provided by the jest.mock() definition in the spec file.
